describe('FileInputComponent', () => {
  describe('test click()', () => {
    // write tests
  });
  describe('test onInput()', () => {
    // write tests
  });
  describe('test onDrop()', () => {
    // write tests
  });
});
